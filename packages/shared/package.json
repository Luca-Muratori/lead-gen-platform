{
  "name": "@lead-gen/shared",
  "version": "0.0.1",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "private": true,
  "scripts": {
    "db:generate": "prisma generate --schema=./prisma/schema.prisma",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "build": "tsc"
  },
  "dependencies": {
    "@prisma/client": "^7.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "prisma": "^7.5.0"
  }
}